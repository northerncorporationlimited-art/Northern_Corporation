# _TO_DELETE

These files are marked for deletion. They are old/experimental pages that are no longer needed for the current site.

## Contents

- `src_app/hero-a/` — Hero variant A ("The Monument" - Full-Bleed Cinematic)
- `src_app/hero-b/` — Hero variant B ("The Split" - Editorial Magazine)
- `src_app/hero-c/` — Hero variant C ("The Reveal" - Scroll-Triggered)
- `src_app/hero-d/` — Hero variant D ("The Frame" - Architectural Showcase)
- `src_app/v1/` — Old V1 home page (duplicate of V2)

## Note

The CSS styles for these components still exist in `globals.css` (hero-a, hero-b, hero-c, hero-d sections). 
Those can be cleaned up when these files are permanently deleted.
